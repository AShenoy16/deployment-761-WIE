import { Request, Response } from "express";

export const getRoleModels = async (req: Request, res: Response) => {
  return res.status(200).json({ message: "TODO implement role model endpoints" });
};
